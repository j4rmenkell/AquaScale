services:
  api:
    build:
      context: ./AquaScale.Api
      dockerfile: Dockerfile
    env_file:
      - ./AquaScale.Api/.env
    # ports, depends_on, etc. added once the API has something to serve

  back-office:
    build:
      context: ./AquaScale.BackOffice
      dockerfile: Dockerfile
    # added once the frontend has something to build

  # field-worker:
  #   build:
  #     context: ./AquaScale.FieldWorker
  #     dockerfile: Dockerfile

  # buyer-portal:
  #   build:
  #     context: ./AquaScale.BuyerPortal
  #     dockerfile: Dockerfile